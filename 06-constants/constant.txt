[상수 설명]
- 상수는 재할당이 불가능해서 좌변이 될 수 없다.
- 상수는 런타임 변수처럼 저장 공간을 잡아두고 읽는 값이 아니라, 컴파일 타임에 값으로 치환(인라인)될 수 있다.
- 컴파일러가 상수 표현식을 미리 계산해 코드에 반영하는 최적화(상수 폴딩)가 일어난다.
    - 예시 :
        const PI = 3.14
        var result int = int(PI * 100)
        이런 코드는 컴파일 시에 그냥 314로 넘어간다.